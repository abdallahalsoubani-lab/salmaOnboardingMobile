import SwiftUI

// Will be implemented in a future prompt
struct FileUploadFieldView: View {
    let field: PageField
    @Binding var value: String
    var error: String?

    var body: some View {
        Text("File Upload Field — Coming Soon")
    }
}
